[project]
name = "Beer-hemian_Rhapsody"
dynamic = ["version"]
description = "Analysis of sound files"
authors = [
    {name = "Alicja Jagielska"},
    {name = "Szymon Książek"}
]
dependencies = [
    "av",
    "numpy",
    "scipy",
    "pandas",
    "matplotlib",
    "IPython",
    "os",
    "glob"
]
requires-python = ">= 3.13"
readme = "README.md"
license = "MIT"
license-files = ["LICENSE.*"]

[build-system]
requires = ["hatchling >= 1.26"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]

[tool.hatch.version]
source = "vcs"
